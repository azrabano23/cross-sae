# Core (the synthetic demo + statistical engine run with just these)
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.1

# Real-data pipeline — install for the model-side, brain-side, and headline runs:
#   pip install timm torchvision nilearn huggingface_hub pillow
# timm>=0.9          # ViT/CLIP activations (phase1_vit_sae.py, headline_model_brain.py)
# torchvision>=0.16  # CIFAR-10 images
# nilearn>=0.10      # Haxby fMRI, brain-side SAE (mvp_brain_sae.py)
# huggingface_hub    # THINGS-EEG2 raw download (headline_model_brain.py)
# pillow>=10.0       # image loading
# NOTE: the THINGS-EEG2 `datasets` loader ships a broken ClassLabel schema;
#       the headline experiment downloads the raw .npy files directly instead.

# Tarjuman FM backbone (experiments/tarjuman_fm_join.py, crosssae/backbone.py):
#   identity + pca arms run with the core deps above (no extra installs).
#   The eeg-fm arm needs a pretrained EEG foundation model + its weights:
#     CBraMod : https://github.com/wjq-learning/CBraMod   (ICLR 2025)
#     LaBraM  : https://github.com/935963004/LaBraM
#   Then: TARJUMAN_FM_WEIGHTS=/path/to/cbramod.pth python experiments/tarjuman_fm_join.py
#   The FM arm is SKIPPED (never faked) if no weights are present.
